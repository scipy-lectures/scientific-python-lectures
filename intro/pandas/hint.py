def hint_1():
    "Prints a hint for the attribute-series exercise"
    print("Try using the `str()` function if you are getting errors regarding the dimensions of `df.shape`...")

def hint_2():
    "Prints another hint for the attribute-series exercise"
    print("The `index` has its own `dtype` attribute...")
